{
  "name": "pizza-management",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "deploy": "vercel deploy",
    "export-schema": "npm run export-schema --workspace=@pizza-management/server"
  },
  "devDependencies": {
    "concurrently": "^9.0.1",
    "turbo": "^1.10.16"
  }
}
